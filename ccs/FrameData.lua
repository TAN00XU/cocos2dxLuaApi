---@meta

---@diagnostic disable: lowercase-global, missing-return, unused-local

--- Cocos API 类：`ccs.FrameData`。
--- 继承：`ccs.BaseData`。
---@class ccs.FrameData : ccs.BaseData
local FrameData = {}
ccs.FrameData = FrameData

--- 调用 `ccs.FrameData:copy`。
---
--- 该声明来自 Cocos Lua 绑定提示桩，函数体为空，仅用于类型检查、补全和文档提示。
---
--- 参数说明：
--- - `baseData`：参数 `baseData`，类型为 `ccs.BaseData`。
---
--- 返回说明：
--- - `self`：当前对象，便于链式调用。
---@param baseData ccs.BaseData 参数 `baseData`，类型为 `ccs.BaseData`。
---@return self 当前对象，便于链式调用。
function FrameData:copy(baseData) end

--- 创建 `ccs.FrameData:create` 对应的对象。
---
--- 该声明来自 Cocos Lua 绑定提示桩，函数体为空，仅用于类型检查、补全和文档提示。
---
--- 返回说明：
--- - `self`：当前对象，便于链式调用。
---@return self 当前对象，便于链式调用。
function FrameData:create() end

--- 调用 `ccs.FrameData:FrameData`。
---
--- 该声明来自 Cocos Lua 绑定提示桩，函数体为空，仅用于类型检查、补全和文档提示。
---
--- 返回说明：
--- - `self`：当前对象，便于链式调用。
---@return self 当前对象，便于链式调用。
function FrameData:FrameData() end
